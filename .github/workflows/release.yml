name: CI

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # region independent
  check-and-build:
    uses: ./.github/workflows/ci.yml
    with:
      build-dist: true
