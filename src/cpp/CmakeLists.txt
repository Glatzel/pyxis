cmake_minimum_required(VERSION 3.10)
project(PyxisProject)

# Set C++ standard
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Enable CUDA (if needed)
find_package(CUDA REQUIRED)

# Include directories
include_directories(${PROJECT_SOURCE_DIR}/include)

# Build a static library
add_library(pyxis_static STATIC ${SOURCES})
set_target_properties(datum_compense_static PROPERTIES
    OUTPUT_NAME "datum_compense"
    POSITION_INDEPENDENT_CODE ON) # Required for shared libraries

# Build a shared library
add_library(pyxis_shared SHARED ${SOURCES})
set_target_properties(datum_compense_shared PROPERTIES
    OUTPUT_NAME "datum_compense"
    POSITION_INDEPENDENT_CODE ON)

# Add source files
set(SOURCES 
src/datum_compense.cpp
src/crypto.cpp
)

# Create a library
add_library(pyxis ${SOURCES})

# Install targets
install(TARGETS pyxis_static pyxis_shared
        ARCHIVE DESTINATION lib
        LIBRARY DESTINATION lib
        RUNTIME DESTINATION bin)

install(DIRECTORY ${PROJECT_SOURCE_DIR}/include/
        DESTINATION include
        FILES_MATCHING PATTERN "*.h")